{
  "name": "@kithena/gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wgc router compose -i graph.yaml -o supergraph.json && router",
    "compose": "wgc router compose -i graph.yaml -o supergraph.json",
    "check": "wgc router compose -i graph.check.yaml -o supergraph.json",
    "typecheck": "tsc --noEmit",
    "typecheck:authoritative": "tsc6 --noEmit"
  },
  "devDependencies": {
    "wgc": "^0.80.0"
  }
}
